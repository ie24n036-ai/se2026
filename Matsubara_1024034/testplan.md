テスト項目 | 入力・操作 | 期待する結果 | 実際の結果 | 判定

※ 判定基準：if 期待 = 実際 → OK / ズレ → NG

| # | テスト項目 | 入力・操作 | 期待する結果 | 実際の結果 | 判定 |
|---|---|---|---|---|---|
| 1 | 新規ユーザー登録ができること | `python app.py register` を実行し、ユーザー名とパスワードを入力する | アカウントが新規作成され、データストレージ（JSON）にハッシュ化されたパスワードが保存されること。 | 保存された | 〇 |
| 2 | 登録したユーザーで正常にログインできること | `python app.py login` を実行し、登録したユーザー名とパスワードを入力する | 認証に成功し、ログイン完了メッセージが表示され、認証トークンが生成・保存されること。 | 正常に作動した | 〇 |
| 3 | 誤ったパスワードでログインが失敗すること | `python app.py login` を実行し、間違ったパスワードを入力する | 認証エラーメッセージが表示され、ログインに失敗すること。 | 間違ったパスワードをうったらエラーメッセージが表示された | 〇 |
| 4 | 新しい学習タスクを追加できること | `python app.py add "Pythonの基本構文" --priority 高` を実行する | タスクID、タイトル、優先度（高）、完了フラグ（False）がデータに登録され、追加完了が表示されること。 | 登録され、追加完了が表示された。 | 〇 |
| 5 | 短いサブコマンド（ls）でタスク一覧がテーブル形式で表示されること | `python app.py ls`（または `list`）を実行する | 未完了のタスク一覧がターミナル上に綺麗なテーブル形式で表示されること。 | 綺麗に表示された | 〇 |
| 6 | 指定したIDのタスクを完了状態にできること | `python app.py done 1` を実行する（タスクID: 1を指定） | 対象タスクの完了フラグがTrue（完了状態）になり、完了メッセージが表示されること。 | 完了状態になり、完了メッセージが表示された。 | 〇 |
| 7 | ポモドーロタイマーが開始され、カウントダウンが表示されること | `python app.py start` を実行する | タイマーが開始され、ターミナル上にリアルタイムで残り時間がカウントダウン表示されること。 | 表示された。| 〇 |
| 8 | タイマー終了時に通知され、学習記録が保存されること | `python app.py start` を実行し、時間が終了するまで待つ | 終了時にテキストメッセージ（およびベル音）で通知され、開始・終了日時、学習時間が自動計算されて保存されること。 | ちゃんと保存された。 | 〇 |
| 9 | 学習状況の統計（レポート）を確認できること | `python app.py summary` を実行する | 日別・週別の合計学習時間が集計表示され、達成率がアスキーアートの進捗バーで表示されること。 | 表示された。 | 〇 |
| 10 | ヘルプオプションにより使い方が確認できること | `python app.py --help` を実行する | 利用可能なサブコマンドの一覧や使い方の説明が1秒以内にレスポンスされること。 | 使い方の確認が出来た。 | 〇 |
